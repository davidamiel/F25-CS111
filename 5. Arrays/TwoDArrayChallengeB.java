public class TwoDArrayChallengeB {

    public static void main(String[] args) {

        // Create a 4x5 double[][] called data.

        // Fill so data[i][j] = (i + 1) * (j + 0.5).

        // For each row: print sum and average.

        // For each column: print min and max.

        // Count how many elements in data are >= 5.0.

        // Identify the index of the row with the highest average.

        // Flatten to a 1D double[] in ROW-major order and print it.

        // Refill data from that 1D array but in COLUMN-major order, then print.

    }
}